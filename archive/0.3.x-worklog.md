---
Task ID: plans-0.3-0.4
Agent: main
Task: Review the stale remote branches (wrong HDR implementations) and pick anything useful, delete them; write 0.3.0-plan (galaxy types: 0.3.0 types, 0.3.1 different types + improved star distributions, 0.3.2 nebula-like objects with proper star distributions, 0.3.3 galaxy age) and 0.4.0-plan (star movement: predefined orbits without gravity, orbit distribution per galaxy type, RTX 3050 moving-star budget, one-vs-two shader architecture, star time slider with camera-coupled / frozen / Myr-per-second zones).

Work Log:
- Fetched and diffed all three remote branches (arena-01a0bdf8-hdr-direct, arena/01a0bdcb-galaxy-flythrough, hdr-cam) against main. All three carry the pre-0.2 HDR lineages (extended Reinhard tonemap, a no-tonemap HDR-direct path, an old labels.js). Review verdict recorded in findings-pitfalls-skills.md.
- Picked: experiments/wgsl-exec-check.js (rewritten against current star-sprite/tonemap shaders — executes the shipping WGSL on the CPU via wgsl_reflect, 23 checks: parse, flux formula parity incl. sub-pixel fade and absMag byte quantisation, behind-camera cull, LUT tint, premultiplied alpha contract, pile-up sweep through the real tonemap fs_main vs a JS pixel model, SDR clamp 1 / HDR ceiling 8, saturation knob); experiments/tonemap-mirror.js (new shared JS model of the tonemap pass, Hable constants read out of the WGSL text — hdr-test.js refactored onto it); the flat-white/white-point measurement methodology (added to findings; re-derived at main's magZero 12 flux scale). npm: package.json + wgsl_reflect dev-only dependency, node_modules gitignored.
- Deleted the three branches from origin (git push origin --delete); remote now has main only.
- Wrote 0.3.0-plan.md: GalaxyModel descriptor (type/seed/params, dynamics stub for 0.4), type→parameter table (B/T, Sersic n, H/L, pitch, arms, gasFraction anchors), DENSITY_PARAMS WGSL mirror strategy (validation contract change documented), Hybrid-mode-only-for-Milky-Way rule, per-type home positions, menu type/seed UI + G key; 0.3.1 full Hubble zoo (E0-E7/S0/Sa-Sd/SBa-SBd/Irr), barred spheroids + arm phase coupling, flocculent arms, irregular FBM + hotspots, population gradients (metallicity colour shifts, arm-ridge young placement, flaring, disc cores), per-type density parity suite; 0.3.2 composite objects (HII+OB association, open/globular clusters King+Plummer, planetary+SNR) with internal star distributions (rejection-free inverters, optional mass segregation), objects buffer block + nebula billboard pass + budgets; 0.3.3 galaxy age (delayed-exponential SFH per type, gas depletion exp(-age/2tau), quenching for E/S0, property-only live regenerate, colour/WD/RGB/nebula-abundance consequences, tests).
- Wrote 0.4.0-plan.md: one closed-form orbit law (pattern / disc / bar / pressure families, Rot_z + wobble with the w(T)-w(0) T=0 identity rule), rotation curves (vFlat 0.225 kpc/Myr, Omega_p 0.041, bar period 154 Myr, solar 229 Myr), family assignment from star-types classes/components, 16-byte record survives (orbit bits in flags 3-4 + reclaimed jitter byte), wobble-consistency rule (amplitude = sigma/omega, time-averaged histograms must match rho within 3%); architecture decision — ONE shader with the law always evaluated (movement ~10 ALU + 2 SFU/vertex over a VS that already does log2/exp2/matrix/LUT), separate static/orbit pipelines kept as a mechanical escape hatch via SHADER_PARTS concat, compute pre-pass rejected; orbit distribution table per galaxy type (family fractions, Omega laws, sigma_v, slider-relevant periods) + spiral winding analysis (arms tighten at 2.1 deg/Myr shear; pattern population + gas ride Omega_p so the galaxy keeps its arms; dynamics.patternLock cosmetic flag); RTX 3050 budget (op-count model: every drawn star can move — 1M solid 60fps, 2M safe, 5M feasible; movement <= 25% of the vertex stage, <= 10% frame at 2M; fill/instances remain the limit; strobe caveat at 99 Myr/s); f32 precision (reduce mod 2pi before sin/cos, error table, T wrap at 2^23 Myr); slider semantics exactly per brief (s=-1/-0.5 camera-coupled at FLIGHT_TIME_GAIN 0.125 Myr/ly, s=0 frozen with snap dead zone, s=0.5..99 Myr/s; T accumulator f64, overlay formats, T key); coherence list (labels/selection/nebulae/Path C share the law via a CPU mirror src/math/orbit.js).
- Docs: findings entries x3 (wgsl_reflect usage, white-point law, branch review), AGENTS.md file list updated, plan.md section 19 pointer added.

Stage Summary:
- python3 scripts/run.py all-tests: 13/13 pass (716 checks; hdr 16/16 on the shared mirror). python3 scripts/run.py wgsl-exec: 23/23 pass (runs the shipping WGSL). Remote branches: main only.
- Deliverables: 0.3.0-plan.md (four releases 0.3.0-0.3.3) and 0.4.0-plan.md (answers: how many stars can move on an RTX 3050 = all drawn ones, 1M/2M/5M tiers; orbit distributions per galaxy type; one shader beats two, with the two-shader escape hatch defined; slider mapping -1 camera-coupled / near-0 frozen / 0.5-99 Myr/s).
- No browser here: the plans are the deliverable of this task; implementation starts with 0.3.0a (GalaxyModel + DENSITY_PARAMS) per the plans' implementation orders.

---
Task ID: 0.3.0-galaxy-types
Agent: main (Arena agent)
Task: Implement 0.3.0 (GalaxyModel descriptor, type→anchor table, four types, Sérsic spheroids, the DENSITY_PARAMS uniform and its validation contract, gas-gated populations, the Milky-Way-only mode rule, per-type homes, type/seed UI) and tighten 0.3.0-plan-galaxy-types-star-distribution.md / 0.4.0-plan-star-move.md so a fork can reimplement 0.3.0 from the plan alone.

Work Log:
- Plans corrected in place, not annotated. 0.3.0: the descriptor now matches what got built (profileId, R0, home as one group, four populations fields, dynamics named exactly as 0.4 will read it, plus an explicit list of what 0.3.0 carries without consuming); "amplitudes are renormalised so the disc reads amp 1.0" became the actual solve (massTotal · unit split by B/T and thickShare, halo = HALO_AMP_UNIT · massTotal); B/T is 1.0 for every E stage (what varies over E0..E7 is flattening b/a = 1 − 0.1E and n); the Sérsic block states rho(s) and the enclosed-mass integral the inverter bisects; the Kennicutt (1981) citation now reports what he found (weak correlation, large scatter) instead of implying a law; a new "Consumers: sampling and populations" section documents the model-first inverter and both gas gates, which had no home in the plan; the delivered-mass/truncation product is spelled out. 0.4: the budget table was recomputed from its own stated derates (its absolute columns were ~2 orders of magnitude off) and now reports the movement increment (+25% vertex ALU, +100% vertex SFU, 0 extra bandwidth) with the fill cliff attributed to sprite size at MAX_SIZE_PX rather than star count; flags match packPacked (bits 3–4 family, 5–7 spare); the params.w paragraph records that the slot holds rAF seconds today, is read by nothing in the star shader, and that renderer-test's frame-time check must move with it; the vacuous |s| < 0.25 snap clause is gone (step is 0.5); R_core → rCore; the °/s column recomputed to 156/1273/220. Doc names in AGENTS.md and plan.md §19 fixed to the files' real suffixed names, and §19 marks 0.3.0 implemented.
- src/math/galaxy.js (new): ANCHORS + TYPE_SPECS for E4/S0/SBb/Sc, interpAnchors (clamped at both ends), massUnit/homeFor/tableGeometry/solveAmps/assemble/applyOverrides, createGalaxy/cycleGalaxyType/galaxyLabel, MILKY_WAY_STRUCTURE as today's numbers verbatim, DENSITY_PARAMS_LAYOUT + packDensityParams. `milkyWay` is provenance, not a string: SBb with no structural override.
- src/math/density.js model-first (rhoThin/rhoThick/rhoSpheroid by profileId/rhoHalo/rhoArm take (model, …); sersicMassFraction + sersicRadiusForFraction + componentMasses/massIntegrals/truncationFractions; the halo mass integral gained the flat-core term it was missing). sampling.js lost its rejection loop (deliveredMasses weights the component pick, each component inverts its own CDF, module-level scratch). star-types.js and nebula.js read model.populations (young window arms.Rs…youngOuterR, GAS_RICH_MIN gate, z-decay thin.H · youngScaleHeight; the stellar nebula term stays ungated).
- Shaders/renderer: SHADER_PARTS['density'] is struct DensityParams + formulas (40 f32, 160 B, @group(0) @binding(5)); PROCEDURAL_GEN declares and threads it. camera.setFrame(model) derives fly and orbit homes from the model, with DEFAULT_FRAME built from GalaxyLib.MILKY_WAY so the boot frame and the default model are one fact. star-sprites.js configureMode/regenerate own the hybrid-vs-game switch (catalog budget, landmark block, cells) and prepare() retains the last non-empty manifest so returning to the preset re-attaches the boot catalog; label-layer.js gained setEnabled; input.js guards form fields and maps G; main.js/index.html/style.css carry the Galaxy menu row, ?type= / ?seed= and the overlay line.
- Tests: new experiments/galaxy-types-test.js (145 checks) wired into scripts/run.py (EXPERIMENTS, TYPES['test'], help); wgsl-validate section 2 rewritten as struct↔layout parity (109); renderer-test +7 on the mode rule (77); landmark-test +3 on setEnabled (46); sampling/density-distribution/star-types-evolution/nebula-placement/tile-encoder/visualize-data retargeted onto galaxy.MILKY_WAY instead of module constants; export-parity's module table and m1-smoke's script/NAMESPACES lists include galaxy.js. Two real src bugs came out of the new tests rather than from review: prepare() dropping the catalog manifest on a mode round-trip, and state.clampedProcedural surviving a regeneration.

Stage Summary:
- python3 scripts/run.py all-tests: PASS — 14 tests, 803 checks, 0 failed (baseline 13/716). python3 scripts/run.py wgsl-exec: 23/23 against the new WGSL. Every pre-existing expected number is unchanged, which is the acceptance criterion for the preset: sampled shares .6262/.1956/.1779/.000265, masses 50.928/15.929/14.472/0.021 (total 81.351), R0 8.178, uniform `8.178 0 0 0 | 2.6 0.3 1 0 | 3.5 0.9 0.12 0 | 1.5 0.5 0.4 1 | 12 1 27 0 | 1 100 3.5 0.0008 | 2 0.2 12 3 | 0 0.5 0 | 0.15 12 1 0 | 25 3 6 0`.
- Needs a browser (not available here): `G` and the Galaxy menu row cycling E4 → S0 → SBb → Sc; E4 must show a pure-procedural spheroid, no landmarks, no catalogue, `galaxy E4 #42  game mode` in the overlay, and switching back must restore the real stars without a reload.

---
Task ID: 0.3.1a-regular-types-and-parity
Date: 2026-09-21
Agent: Arena agent
Task: Split the worklog by release series, establish a next-step logging rule, review 0.3.0, and continue the 0.3.1 plan.

Work Log:
- Moved the original entries verbatim into `archive/0.1.x-worklog.md` (initial planning through true HDR) and `archive/0.3.x-worklog.md` (combined 0.3/0.4 planning and 0.3.0 implementation). `worklog.md` is now the index. The original has no standalone 0.2 implementation entry; no history was invented or duplicated. Added the concise worklog / next-step rule to `AGENTS.md`.
- Reviewed the preceding GalaxyModel step against the release plan and ran its baseline: all 14 test scripts passed. Found coverage gaps behind that result: truncated disc draws inverted the unscaled CDF, spheroid sampling ignored r0, halo sampling omitted the core and ignored the power parameter, the halo field continued past rMax, and centre.z / arm minimum radius / arm phase / derived override fields were inconsistent.
- Updated the release plan before implementation: defined model parity prerequisites and an incremental regular-type-table slice of 0.3.1a. Corrected the full zoo count (19 including Irr), separated full-menu types from the G-key shortlist, specified ordered E axes and interpolation, and explicitly deferred new bar/flocculent/irregular profiles.
- Corrected those density/sampler mismatches in JS and the WGSL mirror. The halo now shares a general core-plus-power-law mass integral with its inverse (including power 3's logarithmic case); disc CDFs are truncated before inversion. Removed the unmodelled 0.01 kpc radial plateau. Empty box-sampler batches no longer emit stale records. Nebula placement respects centre.z and never assigns gas-origin types to quenched models.
- Recompute R0, gasRich and default camera homes after overrides, preserving explicit home fields. Reject inherited object property names as galaxy types. Made the SBb table profile explicit rather than relying on an undefined value falling back to Plummer.
- Expanded the menu to 18 regular types: E0–E7, S0, Sa–Sd, SB0 and SBa–SBd. The original Milky Way preset's authored structural parameters and hybrid-mode rule stay intact. New barred rows currently use triaxial spheroids, not the planned boxy/peanut profile. G still cycles E4 → S0 → SBb → Sc; a menu-only type rejoins at E4. Irr remains unsupported until its field and sampler exist.
- Extended `galaxy-types-test.js` across all 18 types (422 checks), including mass solves, component shares, truncation, E flattening, populations, homes and the mode rule. Added `model-parity-test.js` to the runner and README: independent field quadrature vs compact discs, scaled spheroids and isolated halos, plus translation, phase, override and empty-model regressions. The same test run against the previous checkout fails 37/57 checks; corrected code passes 57/57.
- Extended `wgsl-exec-check.js` to execute the actual density and arm functions for every regular type and an overridden model. Discovered the interpreter's scalar cosh bug; used the equivalent stable decaying-exponential sech² expression in shipping JS/WGSL, without patching the dependency. Added findings and updated the release-plan status pointer.

Stage Summary:
- `python3 scripts/run.py all-tests`: PASS — 15 scripts, 1,115 checks, 0 failed.
- `python3 scripts/run.py wgsl-exec`: PASS — 61/61, including 38 new density/arm checks.
- `git diff --check`: PASS. Historical entries verified against the original worklog. Useful result logs retained; timestamp-only churn removed.
- No browser is installed here. Classic-script loading and menu wiring are tested in Node; actual WebGPU visuals, new type framing and HDR display behaviour still need a browser check. This is the regular-table / parity portion of 0.3.1a, not a completed 0.3.1 release.

Next step suggestion: finish 0.3.1a with the boxy/peanut bar density and matching sampler plus bar-end arm-phase coupling, then flocculent arms and Irr. Keep field/sampler/WGSL parity tests alongside each profile; follow with 0.3.1b population gradients, flaring and disc cores before starting 0.3.2 objects or 0.4 movement.

----------------

**Review of "0.3.1a Shape Machinery / 0.3.1b Population Gradients":** The code for bars, flocculent arms, irregular clumps, flaring, core, and gradients is present, and the baseline suite passes (15 scripts, ~1,115 checks). However, this review turned up **7 actual defects**:

1. **The bar mass integral is a magic-number fudge** — `8π·abc·0.88` (22.12·abc) versus the true closed form `48·abc·Γ(1+1/n)³/Γ(1+3/n)` (30.43·abc, verified by MC): the bar amp is over-luminous by 37.6% relative to its B/T share.
2. **WGSL cross-wiring**: `rhoThin` reads its core radius from the *thick* group, and `rhoThick` reads its flare from the *thin* group — it's latent because the table types happen to have identical values, but it diverges on override.
3. **WGSL flocculent noise is hardwired to seed 42** — flocculent galaxies with seed ≠ 42 diverge CPU vs GPU.
4. **WGSL has no clump/FBM term** — the irregular field breaks the mirror contract.
5. **WGSL `sampleLocalAge`** uses a hard 0.5 kpc cutoff, whereas JS uses a Gaussian ridge gate.
6. **Bar-end arm coupling is broken**: the arm inner edge stays at 0.5 kpc (inside the bar) instead of the bar end, and the ridge winds ~8.5° off the bar end on SBb.
7. **Metallicity gradient**: +1 only kicks in at R ≥ 4L (the plan says 2L), not per-type, can push M→WD, and the "20% redder giants" shift is a no-op (there's no LUT slot redder than RG).

In addition, things that are missing per the plan itself: per-type parity suite (density-distribution is still MW only), wgsl-exec/model-parity coverage of the new machinery, the irregular FBM term + two-stage clump sampler, and plan/worklog updates.

Bar-end arm coupling is broken: the arm inner edge stays at 0.5 kpc (inside the bar) instead of the bar end, and the ridge winds ~8.5° off the bar end on SBb.

Metallicity gradient: +1 only kicks in at R ≥ 4L (the plan says 2L), not per-type, can push M→WD, and the "20% redder giants" shift is a no-op (there's no LUT slot redder than RG).

---
Task ID: 0.3.1b-metallicity-gradient
Date: 2026-09-21
Agent: Arena agent
Task: Finish the metallicity-gradient colour modifier (plan 0.3.1b): per-type steepness, +1 at 2L, clamp at M, dedicated RGe LUT slot for spheroid giants, WGSL mirror.

Work Log:
- `star-types.deriveStar` already applied `floor(R * steep / (2L))` capped at +1, only on thin-disc main sequence, clamped at M so colour never becomes WD/RG. Bulge giants with `uEvolve < 0.2` land on the last LUT entry.
- `star-record.js` added `RGe` after RG (`SPECTRAL_CLASSES` length 10, colour `[255, 106, 76]`). RG remains the reddest classification result; RGe is colour-only.
- `galaxy.js` `GRADIENT_STEEP` anchors: 0 at E, 0.25 at S0, 1.0 at Sc/Sd. Preset `gradientSteep` 0.75. Packed as `populations.w`.
- WGSL `procedural-gen` had copied the JS formula onto `params` (GenParams). That struct has no `populations`/`thin` — the shader would not compile. Switched to `densityParams.populations.w` and `densityParams.thin.x`, with `floor` matching JS.
- Tests already cover Sc 2L step, E4 flat, no MS past M, 20% RGe, LUT length 10.

Stage Summary:
- Metallicity is colour-only as the plan requires. Next remaining 0.3.1b items: arm-ridge young placement, flaring/cores (already in density), per-type density-distribution suite.

Next step suggestion: per-type density-distribution parity for {E4, S0, SBb, Sc, Irr}, then 0.3.2 objects.


---
Task ID: 0.3.1b-density-parity
Date: 2026-09-21
Agent: Arena agent
Task: Continue 0.3.1 with per-type density-distribution parity for E4, S0, SBb, Sc and Irr.

Work Log:
- Updated the 0.3.1 plan before implementation: the disc radial marginal now explicitly includes the soft core, flaring and the exact truncated vertical integral; the sampler, mass integrals and truncation fractions must share that quadrature contract. This replaces the old core-cutoff approximation, which could not match the soft-core field.
- `density.js` now evaluates setup-time disc mass with the same radial marginal as the field. No-core/no-flare models retain the closed exponential integral; flared or cored models use fixed Simpson quadrature. The thin-disc vertical integral is corrected to `4H*tanh(Z/(2H))` at finite height, and the shared `discRadialWeight` is exposed to the sampler.
- `sampling.js` replaces the flat-height / exponential radial shortcut with a 1024-bin monotone CDF table per disc. It is deterministic, rejection-free, and interpolates the field's core + flare + vertical-truncation marginal before drawing the exact per-radius z CDF. This fixes the Sc component-share drift in box samples and brings Irr radial parity under tolerance.
- Corrected the flaring parity fixture to integrate only the model's truncated vertical support. Updated the core fixture to assert finite central mass rather than the obsolete artificial hole. Fixed `wgsl-exec-check.js` to extract the shipping `sampleLocalAge` function from the procedural WGSL part instead of probing a function that is not in the density module.
- Updated the top-level plan status: sampler/model parity and the metallicity colour gradient are complete; the full boxy/peanut bar and remaining population-distribution polish remain.

Stage Summary:
- `python3 scripts/run.py all-tests`: PASS — 15 scripts, all checks passed.
- `node experiments/wgsl-exec-check.js`: PASS — 71/71, including every regular type, Irr, shifted overrides, flocculence, irregular clumps, density/arm execution and `sampleLocalAge` parity.
- `git diff --check`: PASS. No browser is installed here; WebGPU visuals still need a browser check.

Next step suggestion: replace the interim exponential superellipsoid bar with the planned boxy/peanut plus end-cap profile, keeping its mass integral, sampler, WGSL mirror and bar-end arm coupling in parity; then finish the arm-ridge population distribution before starting 0.3.2 composite objects.

---
Task ID: 0.3.1b-boxy-peanut-bar
Date: 2026-09-21
Agent: Arena agent
Task: Replace the interim exponential superellipsoid bar with the planned boxy/peanut profile (inner boxy volume plus exponential end caps), keeping its mass integral, sampler, WGSL mirror and bar-end arm coupling in parity.

Work Log:
- Rejected the tempting "p-generalised-normal direction" sampler by measurement before writing any engine code: normalising a 2D Gaussian by its Lp norm is uniform on the Lp ball only at p = 2 (reduced chi-square 4.88 / 1.007 / 2.68 / 4.81 / 9.91 / 17.71 for p = 1.5 / 2 / 2.5 / 3 / 4 / 6 in 12x12 bins). Recorded in `findings-pitfalls-skills.md` with the numbers.
- `density.js`: `rhoSpheroid`'s bar branch is now the boxy/peanut body in the bar frame — `xi = x_b/(a*r0)`, the peanut stretch `P(xi) = 1 + peanut*xi^2`, the boxy cross-section `tau(xi)^n = 1 - |xi|^n` (n is the boxiness) and the same level radius `s` the other spheroid profiles use, cut at `min(1, truncation.spheroidRadius)`. Density is uniform inside the cross-section and follows `f(xi) = 1` out to `plateau`, `exp(-(|xi| - plateau)/endCap)` beyond. New helpers `barTipRadius`, `barVerticalStretch`, `barLongitudinalProfile`, `barCrossSectionRadius`, `barLongitudinalWeight`, `barLongitudinalIntegral`, `barEnclosedMassFraction`, `lnDiskArea`, `BAR_NONE`; the closed-form superellipsoid mass and `barMassFraction`/`barRadiusForFraction` are gone.
- Truncation is exact on both sides of the cut: `massIntegrals` measures the whole body (its `tip` is 1), `truncationFractions` measures the crop at `barTipRadius`, and the cropped cross-section `tau_R(xi)^n = R^n - |xi|^n` is applied wherever the cut bites, so a `spheroidRadius` override moves the field, the mass and the sampler together.
- `galaxy.js`: `bar: { peanut, endCap, plateau }` is part of every model (a non-bar model carries `density.BAR_NONE`, the degenerate shape nothing reads), assembled into the descriptor, overridable (`OVERRIDABLE` gained `bar`) and packed as the new `barShape` vec4 between `spheroidShape` and `halo`. The spheroid stays the one home for a bar's axes, boxiness (`spheroid.n`, from the new `BAR_BOXINESS` anchor) and tilt; `tableGeometry` gained `spheroidIndex`, a stage anchor for `r0` and the bar anchors `BAR_PEANUT`/`BAR_PLATEAU`/`BAR_END_CAP`. Arm coupling is unchanged in form and now exact: `minRadius` is the bar's own end (`axes[0]*k*r0`).
- `sampling.js`: the bar gets its own rejection-free draw — `|xi|` by inverting the field's longitudinal marginal (1024-bin Simpson, the same quadrature contract the discs use), a fair sign, then a uniform point in the cross-section at that `xi` (slice marginal `(1-|y|^n)^(1/n)` by table, then a uniform coordinate). The shared table inverter `sampleTable` now backs the disc radial draw too.
- `src/render/shaders.js`: `DensityParams` gained `barShape`, and `rhoSpheroid`'s bar branch mirrors the JS field expression for expression.
- Tests: `galaxy-types-test.js` §6b now checks the ellipsoid limit (n = 2, no peanut, no cap = 4/3*pi*a*b*c*r0^3), a 64^3 midpoint sum of `rhoSpheroid` against `massIntegrals` (agreement < 5e-3), a truncation override that crops the delivered mass, and that no sampled bar star leaves the body. `model-parity-test.js` gained the bar's major-axis and vertical marginals (the vertical one integrated against the sample's own xi marginal, which is what tests the peanut), the end-cap profile, the lobe-is-thicker-than-the-centre field probe and the pointed tip. `density-distribution-test.js` gained a table bar (SBa) to the per-type suite. `wgsl-exec-check.js` probes four points inside the bar frame (end cap, lobe, off-axis cross-section, just past the tip) for every barred model.

Stage Summary:
- `python3 scripts/run.py all-tests`: PASS — 15 scripts, 1,243 checks, 0 failed.
- `node experiments/wgsl-exec-check.js`: PASS — 71/71 (executed density WGSL vs JS, worst relative error 4.3e-7 at the new bar probes).
- SBa per-type parity: R TV 0.0094, z TV 0.005, component shares within 0.37% of the field's own box integral.
- `git diff --check`: PASS. No browser here: the bar's look still needs a browser check.

Next step suggestion: finish the arm-ridge young-population distribution (0.3.1b) — young stars placed as a half-normal ridge (sigma from arm width and pitch) instead of "inside the tube" — then start 0.3.2 composite objects.

---
Task ID: 0.3.1b-arm-ridge
Date: 2026-09-21
Agent: Arena agent
Task: Finish 0.3.1b's arm-hugging young population — O/B stars born on the arm ridge, with a width that is the model's own pattern instead of a distance tuned for the Milky Way — and put the gas layer on the same lane.

Work Log:
- `density.js`: new `ARM_RIDGE_FRAC = 0.12` and `armRidgeWidth(model, R) = ARM_RIDGE_FRAC * (2*pi*R*sin(pitch)/m) / (1 + amp)` — the m-fold logarithmic spiral's perpendicular wavelength, narrowed by the arm contrast (a stronger arm compresses its gas harder). Exported beside `distanceToNearestArm`.
- `star-types.js`: the thin-disc arm-young branch is the half-normal gate `exp(-0.5*(distToArm/armRidgeWidth(model, R))^2)`, replacing the fixed 0.3 kpc width. Measured per star (z = distToArm / sigma(R)) the young population is a half-normal for every type — median 0.664-0.673 against the ideal 0.6745, P(z<1) 0.682-0.690, P(z<2) 0.952-0.957 for Sa/Sb/Sc/Sd — so the branch now reads the pattern rather than a number.
- `src/render/shaders.js`: the procedural `sampleLocalAge` mirrors the expression exactly (`params.arms` already carries m, amp, pitchDeg, Rs), so the GPU path stays in parity with the CPU one.
- `nebula.js`: the gas layer reads the same lane. The arm probability boost is the same Gaussian in the model's ridge width, HII regions are admitted within one ridge width, reflection nebulae within `NEBULA_ARM_REACH = 2.4` and dark clouds within `NEBULA_DARK_REACH = 4.6` — the numbers that reproduce the Milky Way's tuned 0.8 / 1.5 kpc at R = 5, where its ridge is 0.33 kpc, so a wider or tighter pattern scales instead of inheriting an MW-sized lane.
- Tests: `star-types-evolution-test.js` gained the per-star half-normal suite (Sa, Sc: median z 0.671 / 0.658, P(z<1) 0.679 / 0.687, P(z<2) 0.9554 / 0.9555) plus "the ridge is the pattern's, not a constant" (Sa 0.320 kpc against Sd 0.435 at R = 8) and "it widens in proportion to R"; `nebula-placement-test.js` gained the exact gas-lane check (the arm probability falls by e^-1/2 one ridge width off the crest, Sa and Sd, ratio 0.6065) and its halo-leak guard moved from 5% to 6% because the lane widens with radius (60 of 1200 against 59 at the fixed width); `wgsl-exec-check.js` gained two `sampleLocalAge` probes on opposite sides of the old 0.3 kpc cut, so the branch decision itself is sensitive to the model's width.
- Documented before/with the code: `plan.md` §Spiral arm modulation and the 0.3.1 plan's arm-hugging item now carry the formula, and `findings-pitfalls-skills.md` records the per-star validation lesson (a mixture over sigma fakes a heavy tail: median z 0.62 and P(z<2) 0.89 with one global sigma) and the two measurement traps (the gate reads azimuthal arc distance, not the perpendicular one; never sort a distance array and its radius array separately).

Stage Summary:
- `python3 scripts/run.py all-tests`: PASS — 15 scripts, 1,249 checks, 0 failed.
- `node experiments/wgsl-exec-check.js`: PASS — 71/71.
- Young fraction of the thin disc by type: Sa 2.3%, Sb 3.3%, Sc 5.0%, Sd 7.4% — the branch scales with gas and arm contrast rather than one fixed lane.
- `git diff --check`: PASS. No browser here: the ridge's look in the renderer still needs a browser check.

Next step suggestion: 0.3.1's remaining polish is done, so start 0.3.2 — composite objects (HII region + OB association, open and globular clusters, planetary nebula + central star) with `placeObjects`, the internal profile inverters and the objects block in the star buffer.

---
Task ID: 0.3.1b-arm-spiral-geometry
Date: 2026-09-21
Agent: Arena agent
Task: Draw the field itself to check the arm-ridge work, and fix what the drawing exposed: the arm modulation's wavenumber was `tan(pitch)` instead of `m/tan(pitch)` (a fan of radial spokes), and the distance-to-ridge the lane widths are written in was the azimuthal arc rather than the perpendicular distance.

Work Log:
- Found by looking, not by testing: extending `scripts/viz-galaxy.py` to give the edge-on XZ slice a panel of its own (a dedicated inner-kpc grid for the bar's peanut, `densityGridXZ` from `experiments/visualize-data.js`) put the top-down map and the young-star overlay side by side, where the cyan guide curves from the arm model clearly did not lie on the arms. The guide curves were right; the field was wrong.
- `density.js`: new `armWavenumber(model) = m / tan(pitch)` and `armsArmed(model)`; `armFactor` and `distanceToNearestArm` read them (the `amp === 0 || m === 0` test also gained the `pitchDeg > 0` guard a degenerate pitch needs). `distanceToNearestArm` is now the *perpendicular* distance — wrap the phase residual into one turn and divide by `hypot(m, K)`; the m-branch loop is gone, and with it the aliasing risk at large m. New `armRidgeAzimuth(model, R)`, the zero set of that distance, so tests, the visualizer and (later) object placement do not each re-derive the logarithm.
- `galaxy.js`: the bar-end `phase0` solve uses the same convention (and is wrapped into one turn, which also keeps the WGSL mirror's f32 residual precise). `sampling.js`: the azimuthal profile sampler reads `density.armsArmed`/`density.armWavenumber` instead of its own copy of `tan(pitch)`. `src/render/shaders.js`: the WGSL mirrors the new expressions (`armWavenumber`, `armsArmed`, the perpendicular distance), so the GPU field and the CPU field still come from one formula.
- Tests: `galaxy-types-test.js` gained the pitch-angle check (crest azimuth at two neighbouring radii; recovers every anchor pitch within 0.05 deg, 6.98-22.01 against 7-22) and the crest-spacing check (consecutive crest radii along a ray are `e^(2*pi/K)` apart, recovering the `lambda` the widths are written in to 0.03 %), both measured from the field rather than from the formula. The bar-end and phase-shift fixtures now read `armRidgeAzimuth`/`armWavenumber` instead of their own `tan(pitch)` arithmetic, `nebula-placement-test.js` steps off the crest *perpendicular to it* and measures the landed distance against the crest line itself (an azimuthal step would have measured the pitch angle, not the width), and `star-types-evolution-test.js` compares classes in units of the model's own lane: `classVsArmDistance(stars, model)` now takes the model, restricts to the star-forming annulus and reports `meanZ` / `fracInLane`, because a fixed 0.5 kpc cut reads O/B 0.78 against M 0.49 (a factor 1.6, half the lane inner-disc and a fifth of it outer-disc) where the lane-relative reading is 0.70 against 0.23.
- Consequence measured, not guessed: the corrected gate admits the documented ~0.3 kpc lane instead of a `1/sin(pitch)`-too-wide arc, so the young fraction of the thin disc rose from 2.3 % to 18 % (MW preset) while the O/B census stayed at 0.1 % of all stars; the per-star half-normal shape is unchanged (median 0.65, P(z<1) 0.70, P(z<2) 0.96). The nebula reaches (2.4 sigma, 4.6 sigma) now mean the 0.8 / 1.5 kpc they were documented as.
- `scripts/viz-galaxy.py` and `experiments/visualize-data.js`: three panels — the density map with star classes and nebulae, the arm modulation (`rho` divided by its own azimuthal mean per radius, which is what exposed the bug: a 15-25 % ripple on an orders-of-magnitude falloff is invisible on the raw field and unmissable on the ratio), and the zoomed edge-on slice for the bar's peanut. The type argument (`node experiments/visualize-data.js SBa`) and the arm guide curves now follow the field's own convention.
- Docs: `plan.md` §Spiral arm modulation and the 0.3.1 plan's arm items carry `K = m/tan(i)`, the perpendicular-spacing identity and the new measurements; `findings-pitfalls-skills.md` gained the wavenumber entry (with the visualizer lesson: a design doc is not a test, and 0.3.0 + 0.3.1 round-tripped the same wrong formula) and the older distance-to-ridge entry is marked superseded.

Stage Summary:
- `python3 scripts/run.py all-tests`: PASS — 15 scripts, 1,263 checks, 0 failed (up 14 from 1,249: eight winding checks, five spacing checks, one class-vs-lane rewrite in `star-types-evolution`).
- `node experiments/wgsl-exec-check.js`: PASS — 71/71; the executed WGSL arm factor and distance match JS to 1.6e-6 worst case across the type table.
- Measured pitch against anchor: Sa 6.98 / Sb 10.01 / Sc 14.99 / Sd 22.01 deg (anchors 7 / 10 / 15 / 22); crest spacing against `lambda`: agreement 0.02-0.03 %.
- `git diff --check`: PASS. The visualizer rendered MW, SBa and Sc; the browser check of the renderer's own look is still outstanding.

Next step suggestion: 0.3.2 composite objects — `placeObjects` over the arm lane this task just fixed (OB associations and HII regions belong on the crest, open clusters nearby, globulars in the halo), the internal profile inverters, and the objects block in the star buffer.

---
Task ID: 0.3.2a-composite-objects
Date: 2026-09-21
Agent: Arena agent
Task: Implement 0.3.2a — composite objects as star concentrations: `placeObjects` over the arm lane, internal profile inverters (Plummer / King / fractal), age-overridden member derivation, and the objects block in the star buffer. No gas rendering yet (0.3.2b).

Work Log:
- Corrected the 0.3.2 plan in place before coding: concrete per-type probabilities (`pHII` == the loose `pGas`, disc-wide `pOpen`, spheroid-share `pGlobular` via a setup-time `objectContext`, unchanged `pPlanetary`, thin×arm `pSNR`), the `placeObjects(model, seed, N, box)` signature (the draft had the arguments backwards), corrected Plummer inversion (`r = a/sqrt(u^-2/3 - 1)` — the draft's `a·u^1/3(1-u)^-1/3` inverts `M = r^3/(r^3+a^3)`, not Plummer), the concrete King form (`((1+x^2)^-1/2 - (1+12^2)^-1/2)^2`, 1024-bin Simpson like the discs/bar), the fractal's bounded retries, the buffer order `[global][landmarks][objects][local][catalog]` (fixed-on-regenerate blocks stay contiguous, so one fixed upload and one dynamic upload), `OBJECT_COUNT_DEFAULT = 400`, and proportional apportionment of richness over the fixed block.
- `src/math/objects.js` (new): `placeObjects` (4096-star candidate batches from the field sampler, whole-galaxy when `box` is null, empty model yields no objects instead of looping on empty batches), `objectProbabilitiesAt` (reuses the nebula lane constants, now exported, and computes dominance from the one field evaluation instead of a second), `plummerRadius` / `kingRadius` / `sampleMemberOffset`, `objectQuotas` (cumulative rounding: exact total, min-1 per non-empty object, first-win only when even that cannot fit), `writeObjectMembers` (centre + offset, R/arm recomputed at the member, `FLAG_VISIBLE` StarPacked records), `summariseObjects`.
- `star-types.js`: factored the mass+age → state/class/colour core into `evolveStar`; `deriveStar` is unchanged behaviourally (pinned by a new exact-equivalence check), `deriveStarWithAge` serves coeval members (HII/open as thin disc, globulars as halo — old age alone removes O/B/A/F), `derivePlanetaryCentral` derives the post-AGB star (O, 30–100 kK, L 10^2–10^4). `sampling.js` exports its table inverter and sphere-direction draw for the member samplers rather than duplicating them.
- Measured before pinning: MW-box mix 551/152/120/184/193 (planetary/open/HII/SNR/globular), HII mean arm 0.275 kpc vs open 0.766, globular mean R 1.88 with 81% bulge-dominated, Sc HII+open 53% whole-galaxy (47% in the ±20 kpc box, which overweights the bulge — shares are measured whole-galaxy), E4/S0 zero HII/open, Plummer/King enclosed fractions within 0.005 of theory, fractal 99.71% above-noise inside exactly 2.5 R_e (retries 8→10 bought the margin over the 99% bar), HII O/B 0.72%, globular RG+WD 3.3% with zero O/B/A/F.
- Renderer: objects block between landmarks and the dynamic region (offsets shift, dynamic upload untouched), placed whole-galaxy on regenerate with a domain-separated seed, drawn always (`MASK_VISIBLE` culls unfilled capacity), overlay names the block. `renderer-test.js` updated to the five-block layout plus a defaults check (omitted options → 400 objects / 20000 members); `object-placement-test.js` (new, 45 checks) wired into `scripts/run.py`, the experiments README, `m1-smoke` and `export-parity`.
- Tooling gotcha, recorded in findings: parallel `edit_file` calls to the same file race (last write wins, the rest report success) — five plan edits landed one, and a rate change was silently lost until identical mixes exposed it. Same-file edits now go sequentially or as one asserted script.

Stage Summary:
- `python3 scripts/run.py all-tests`: PASS — 16 scripts, 1,319 checks, 0 failed (up 56: object-placement 45, renderer +5, export-parity +5, m1-smoke +1).
- `node experiments/wgsl-exec-check.js`: PASS — 71/71 (no WGSL change; the members are CPU-derived StarPacked).
- `git diff --check`: PASS. Timestamp-only log churn reverted; kept logs are export-parity, m1-smoke, renderer (new checks) and object-placement (new).
- No browser here: clusters read as concentrations in the tests' histograms, but the on-screen look (association sparseness, globular saturation, inner-galaxy crowding) still needs a browser check.

Next step suggestion: 0.3.2b — the nebula billboard pass (per-object quads from the carried shell sizes, screen-size cull below ~4 px, distance cull past 5 kpc), with the renderer-test billboard checks the plan defers to it; then 0.3.3 galaxy age.
