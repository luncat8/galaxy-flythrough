# 0.3 — Galaxy Types, Stellar Objects & Galaxy Age

The engine today renders one galaxy: the hard-coded Milky Way in `src/math/density.js`
(thin/thick disc, triaxial bulge, power-law halo, two-armed spiral modulation) with the
stellar populations of `src/math/star-types.js` and the placement rules of
`src/math/nebula.js`. Everything downstream — sampling, the WGSL density mirror, nebula
placement, the local gap-fill — reads those constants. This plan parameterises the model
so the same pipeline flies through different galaxy types, gives those types proper
stellar objects (nebulae *with* the stars that belong to them), and finally adds galaxy
age as a population-synthesis knob.

Four releases, each shippable alone:

| Release | Theme | One line |
|---|---|---|
| 0.3.0 | Galaxy types | The `GalaxyModel` descriptor, type → parameter table, type selector UI. Four types prove every axis. |
| 0.3.1 | Different types, improved star distributions | The full Hubble zoo: E0–E7, S0, Sa–Sd, SBa–SBd, Irr. Barred forms, flocculent arms, metallicity gradients, clumpy irregulars. |
| 0.3.2 | Objects like nebulae with proper star distributions | Composite objects: HII region + OB association, open and globular clusters, planetary nebula + central star — each with its own internal density profile. |
| 0.3.3 | Galaxy age | Star-formation history per type; the population (colours, giants, nebula abundance) and the gas left in the disc follow the galaxy's age. |

Ground rules inherited unchanged: density field is the source of truth, truncation lives
in the model, stable hash (no PRNG state), no per-frame allocations, `file://` classic
scripts, WGSL as mirrorable parts. 0.4 (star movement) consumes the descriptor this plan
defines; see `0.4.0-plan-star-move.md`.

---

## 0.3.0 — Galaxy types (the framework)

### The GalaxyModel descriptor

One plain object, created once per galaxy, threaded through every consumer
(`density.js`, `sampling.js`, `star-types.js`, `nebula.js`, the WGSL mirrors):

```js
// src/math/galaxy.js (new)
GalaxyModel = {
	seed: 42,                    // master hash seed; same seed + params = same galaxy
	type: 'SBb',                 // canonical type string, see the table below
	T: 3,                        // de Vaucouleurs stage index (see below)
	barred: true,
	milkyWay: true,              // the mode rule: is this the unmodified preset? (see below)

	// Structural parameters. Exactly the fields density.js hard-codes today.
	thin:  { L, H, amp },
	thick: { L, H, amp },
	spheroid: { profile, profileId, a, b, c, r0, n, amp, tiltDeg },   // bulge, E body or bar
	bar:      { peanut, endCap, plateau },   // read only by the bar profile (0.3.1)
	halo:  { a_h, rMax, power, amp },
	arms:  { m, amp, pitchDeg, Rs, phase0, minRadius, flocculence },  // amp 0 = smooth disc
	truncation: { discRadius, discHeight, spheroidRadius },  // spheroidRadius is in units of s

	// Population knobs, consumed by star-types.js and nebula.js.
	populations: {
		gasFraction,      // 0 (quenched) .. 0.5 (gas rich)
		gasRich,          // gasFraction >= GAS_RICH_MIN — the gate both consumers read
		youngOuterR,      // outer edge of the star-forming annulus (inner edge is arms.Rs)
		youngScaleHeight, // HII/OB vertical thickness, as a fraction of thin.H (nebulae)
		spheroidOld,      // reserved: metal-poor old spheroid (0.3.1 gradients)
	},

	// Reserved for 0.4 and defined from the start, so the descriptor never has to grow to
	// accommodate it. 0.3.0 carries these numbers and consumes none of them.
	// Speeds are kpc/Myr (1 kpc/Myr = 978 km/s), so vFlat 0.225 is a 220 km/s disc.
	dynamics: { vFlat, rCore, omegaPattern, sigmaThin, sigmaThick, sigmaSpheroid,
				  spinLambda, patternLock },

	R0: 8.178,                   // |centre| — the Sun-circle radius, 0 when galactocentric
	scaleKpc: 1.0,               // one overall size multiplier (see Scaling)
	home: { position, yaw, pitch, orbitTarget, orbitDistance, orbitName },   // per-type start
}
```

`profileId` is `density.PROFILE_PLUMMER` / `PROFILE_SERSIC` / `PROFILE_BAR`: a profile is authored as a string
and compared as a number, on the CPU and in the packed uniform, so `assemble` derives the
selector from the string — one place, covering the preset, the table and overrides alike.
`barred`, `arms.flocculence`, `populations.spheroidOld` and all of `dynamics` are carried in
0.3.0 and read by nothing: the *shape* of the descriptor is the deliverable, and 0.3.1/0.4
fill it in without a migration. `bar` follows the same rule: every model carries the group
(a non-bar model carries the shape that degenerates to a plain boxy ellipsoid) and only the
bar profile reads it, so the packed uniform has one layout for every type.

`createGalaxy({type, seed, overrides})` builds a model. The Milky Way preset is `SBb`
(T = 3, barred) and it is *authored*, not derived from the anchors: it carries today's numbers
verbatim, so the existing suite must pass unchanged on the default model. Its authored bulge
share is 0.182 where the B/T anchor says 0.15 at T = 3 — the preset is authoritative for the
Milky Way, the curve is what the other types use. A structural override means "not the
preset": the model comes from the table with the override applied on top, shallow per group,
and `milkyWay` goes false.

### Type → parameter table

The type string maps to `T` (de Vaucouleurs stage, −5…10) plus `barred`. 0.3.0 ships
**four types**, chosen so every parameter axis is exercised, not for coverage:

| Type | T | What it proves |
|---|---|---|
| `E4` | −2 | No disc, no arms, no gas — the spheroid-only path. |
| `S0` | 0 | Disc + spheroid, no arms, quenched — the "disc with armAmp 0" path. |
| `SBb` (Milky Way) | 3, barred | The default. Arms + all four components; `barred` is authored and carried but unconsumed until 0.3.1 models the bar. |
| `Sc` | 5 | Late-type: thin small spheroid, open arms (high pitch, low contrast spread over more arms), gas rich. |

0.3.1 replaces this quartet with the full table; the mapping below is the one it extends.

First-order parameter mapping, tuned so the *shapes* read correctly at fly-through scale.
These are the values the project adopts, not measurements it reproduces: Kennicutt (1981)
measured pitch angles across 113 Sa–Sc spirals and found the correlation with Hubble type to
be weak with large scatter (it tracks shear and maximum rotation velocity better), and B/T
spans about an order of magnitude inside a single stage. The monotone curves below are the
mean of those trends — defensible as a shape dial, and 0.3.1's per-type scatter is what makes
the zoo read as populations rather than as a ladder.

```
spheroid/disc mass ratio B/T:  E 1.0 · S0 0.5 · Sa 0.3 · Sb 0.15 · Sc 0.05 · Sd 0.02
                               (every E stage is 1.0 — an elliptical has no disc; what varies
                               over E0..E7 is flattening, b/a = 1 − 0.1·E, and the Sersic n)
spheroid Sersic n:            E 4 (de Vaucouleurs) · early bulges 2 · late pseudobulges 1
disc H/L ratio:               S0 0.15 · Sa 0.12 · Sb 0.09 · Sc 0.07 · Sd 0.06   (early types puffier)
arm pitch (deg):              Sa 7 · Sb 10 · Sc 15 · Sd 22                        (Kennicutt 1981)
arm number m:                 2 (grand design) .. 4 (flocculent blend)
arm contrast amp:             S0 0 · Sa 0.15 · Sb 0.20 · Sc 0.25 · Sd 0.30
gasFraction:                  E 0 · S0 0.02 · Sa 0.08 · Sb 0.15 · Sc 0.30 · Sd 0.45 · Irr 0.5
```

Each structural parameter is a monotone function of `T`, interpolated through these
anchors by a flat data table in `galaxy.js` — no if-else chains (AGENTS.md style rule).

Amplitudes are solved, not renormalised to a cosmetic 1.0: `thin.amp = 1` is a property of the
preset alone. Every table type gets the split its authored numbers imply, from the untruncated
mass integrals of the four components:

```
total        = massTotal · unit          // unit is the preset's own total, so 1.0 means MW
spheroid     = total · B/T
thin + thick = total · (1 − B/T)         // divided between them by thickShare
halo.amp     = HALO_AMP_UNIT · massTotal // the preset's halo/disc ratio; 0 when spec.halo is false
```

`density.truncationFractions()` then says how much of each component the truncation keeps, and
`sampling.deliveredMasses()` is the product — the weights the sampler uses. A type whose
spheroid is cut at 8 effective radii therefore does not receive the full integrated share of
its stars, and the counts agree with the field; a type with no disc gets `thin.amp =
thick.amp = 0` from the same arithmetic, so no consumer needs an "if E-type" branch.

### Scaling

Types differ in size as well as shape. Every length in the model is authored in model
units and multiplied by `scaleKpc` at construction:

```
E4:    scaleKpc 1.5   (R_e ≈ 1.5 kpc body — compact; spheroid semiaxes scale together)
S0:    scaleKpc 1.0   (disc L ≈ 2.5 kpc)
SBb:   scaleKpc 1.0   (L = 2.6 kpc — exactly today's Milky Way)
Sc:    scaleKpc 1.4   (L ≈ 3.5 kpc, puffier arms)
```

`GALACTIC_R0` and `GALACTIC_CENTRE` become model properties `R0` and `centre` (a sun frame
only exists for the Milky Way preset — see the mode rule). `density.js` stops exporting
module-level galaxy constants: every field function takes `(model, …)` first, and
`bulgeEllipsoidRadius`/`rhoBulge` become `spheroidEllipsoidRadius`/`rhoSpheroid` over the
`model.spheroid` group. The blast radius is every call site, not just the two that look like
one: `cell-manager.js`'s expected-density probe, `star-sprites.js`'s gap-fill, `camera.js`'s
orbit centre and `nebula.js`/`star-types.js` all read model fields now.

### The spheroid profile

`plummer` stays (it is what the bulge is today and what the sampling inverter knows).
`sersic` is added for E/S0 bodies, with `n` from the anchor curve:

```
rho(s) = amp · exp( −b_n · (s^(1/n) − 1) ),   s = the triaxial ellipsoidal radius / r0
b_n    = 2n − 1/3                             (the standard approximation, exact enough here)
M(<s)  ∝ γ(3n, b_n · s^(1/n))                  (lower incomplete gamma, Lanczos log Γ)
```

Both profiles are truncated at `truncation.spheroidRadius` in units of `s` — the
generalisation of `TRUNCATION.bulgeRadius`, and enforced *in the field* (`rhoSpheroid`
returns 0 past the cut) because the sampler draws inside what the field says. Sampling is
inverse-CDF, not rejection: the disc and Plummer branches are closed-form, the Sérsic branch
bisects `M(<s)` in 32 deterministic steps, the same contract `sampleDiscRadius` uses. The
enclosed-mass fraction the inverter is handed comes from the same integral the truncation
fraction reports, so a star is never drawn where the density is 0 and the delivered mass
agrees with the sampled counts to the last decimal.

### Density is parameterised; the WGSL mirror follows

This is the one architectural consequence that must not be deferred. `SHADER_PARTS
['density']` used to re-type the constants so `wgsl-validate.js` could diff the WGSL against
`src/math/density.js`. With a parameterised model those numbers cannot be in the source text
any more, so the mirror contract changes shape:

- The WGSL part carries the *formulas* plus one `struct DensityParams`: ten `vec4f` groups
  (centre, thin, thick, spheroid, spheroidShape, halo, arms, armShape, populations,
  truncation) — 40 f32, 160 B, bound at `@group(0) @binding(5)`. Only the component indices
  and the profile selector stay compiled in.
- `src/math/galaxy.js` owns the layout as data: `DENSITY_PARAMS_LAYOUT` is one list of
  `{name, source, fields}` entries that `packDensityParams(model, out)` walks, `wgsl-validate`
  diffs against the struct text, and the compute pass binds. A field the shader does not read
  stays out of the uniform rather than riding along unused.
- `wgsl-validate.js` pins group names, order, size, the binding slot and that every group is
  actually read. It cannot check arithmetic; `wgsl-exec-check.js` parses the composed module
  and the numeric model tests own the arithmetic.
- `packDensityParams` writes into the caller's `Float32Array`, so a rebuild packs into the
  buffer the renderer already owns. `procedural-gen` declares the uniform and threads
  `densityParams` into every density call; uploading it from the compute path happens when the
  precomputed field is wired (Milestone 3), not before.

### Consumers: sampling and populations

`sampling.js` becomes model-first and loses its rejection loop. `sampleGalaxyStars(model, seed,
count, out)` picks a component from the delivered masses, then inverts that component's own
CDF — exponential disc radius, sech² and Laplace z, Plummer or Sérsic radius, uniform azimuth
and an arm replica for the thin disc — with the same 32-step bisection contract everywhere, so
the histogram of the stars is the field rather than an approximation of it. `sampleStarsInBox`
keeps the box, `deliveredMasses(model)` is the weight list, and the module-level truncation
helper it used to duplicate is `density.truncationFractions`. No rejection loop, no per-star
allocation: the scratch `Float64Array` is module-level.

`populations` is read by exactly two consumers, both from the model rather than from a
threshold in local code:

- `star-types.sampleLocalAge(model, component, distToArm, R, …)` — a gas-poor thin disc has no
  young branch at all (`logNormal(9, 0.4)` everywhere), and where it is star-forming, the
  arm-young window is `arms.Rs < R < populations.youngOuterR` instead of the hard-coded 3…12.
- `nebula.nebulaProbabilityAt(model, …)` — the gas term is scaled by `gasFraction / 0.15` and
  zeroed by `gasRich === false`, its vertical decay is `thin.H · youngScaleHeight`, and its
  spiral region is the model's `Rs … youngOuterR`. The stellar term is not gated: a quenched
  disc still has planetary nebulae and SNR, it has no HII regions.

Because both read `model.populations`, a `gasFraction` override on any type moves the stellar
content and the nebula content together, and the type string is not consulted anywhere below
`galaxy.js`.

### Mode rule: catalog and landmarks are Milky Way only

The Gaia subset, `landmarks.js` and `constellations.js` describe real stars in the real
Milky Way. They are meaningless in an E4. The rule is one boolean on the model —
`milkyWay`, true only for `SBb` with no structural override — and both consumers read it:

```
model.milkyWay  →  Hybrid mode: catalog streaming on, landmark block written, labels live
otherwise      →  Game mode: catalog budget 0, no landmark records, label layer disabled
```

It is a mode, not a second renderer: the same buffer layout, bind group and draw call, with
two blocks empty. `star-sprites.js` enforces it (`regenerate(model)` recomputes it), and
`main.js` uses it for the two things it owns — whether to pick a named star, and whether
`labels.setEnabled()` is on.

The sun-centred coordinate frame (`centre` at +X, R0 = 8.178) is likewise a preset property;
every table type is galactocentric, with the centre at the origin. `homeFor(model)` places the
start view from the model's own size:

```
Milky Way preset  (0, 0, 0.005), level, yaw 0 — unchanged, and H orbits the Sun from 10 pc
disc types        (0, −3.5·L, 0.5·L)   — the classic three-quarter view of the whole disc
E types           (0, −4·R_e, 1.2·R_e) — the same framing, one step outside the body

`isDisc` here is `thin.amp > 0`, not the type string: a disc the solve left empty is framed
as the spheroid it has become.
```

`model.home` carries position, yaw, pitch **and** the orbit-mode home (`orbitTarget`,
`orbitDistance`, `orbitName`): the preset's is the Sun at 10 pc, any other type's is the model
centre at the same stand-off as the fly view, because a generated galaxy has no sun to circle.
`camera.setFrame(model)` installs all of it (`GALACTIC_CENTRE_TARGET` is derived from
`GalaxyLib.MILKY_WAY` at load, so the boot frame and the default model are one fact, not two).

### UI

The settings menu (Tab) grows a **Galaxy** section under the display sliders:

```
Type  [ E4  S0  SBb  Sc ]        SBb #42 (catalog)
Seed  [ 42            ]  [ Apply ]
```

The `<select>` is filled from `GALAXY_TYPE_CYCLE` at boot, so adding a type to `galaxy.js`
adds it to the menu without touching `main.js`; the readout names the type, the seed and
which mode the model is in. Changing either control regenerates: rebuild the procedural
field, rebuild gap-fill, switch mode, and re-home the camera. `?type=` and `?seed=` do the
same at boot (`readParams` grows one string parameter; the seed default comes from
`GalaxyLib.DEFAULT_SEED` rather than a literal here).

Regeneration is synchronous, same cost as the startup field build (~0.8 s at 300k: 0.6 s of
sampling, 0.2 s of deriving records) — acceptable for a deliberate action, and there is no
half-swapped state to hide behind a progress bar.

Keyboard: `G` cycles the four types (a one-shot action in `pressMap`, so `e.repeat` cannot
rebuild the field at the key-repeat rate). The overlay names the galaxy on its own line:
`galaxy SBb #42   hybrid mode`. Because the menu now holds a `<select>` and a number field,
`input.js` lets form targets keep their keys: every key except `Tab` is ignored while an
`INPUT`/`SELECT`/`TEXTAREA` has focus, so digits can be typed and a slider can be nudged
without flying the camera.

### Tests (0.3.0)

- `experiments/galaxy-types-test.js` (new): the table is complete and self-consistent; the
  Milky Way preset reproduces today's constants to the digit; the anchor curves are
  monotone in T and clamp at both ends; the amplitude solve lands on the authored massTotal
  and B/T for every table type; `deliveredMasses` is the model mass times the truncation
  fraction; a sampled field puts no star outside its component's truncation; the Sérsic
  enclosed-mass integral and its inverter round-trip; `packDensityParams` writes the layout it
  claims; and the gas threshold decides both the young population and the nebulae.
- `wgsl-validate.js`: `DensityParams` struct parity against `DENSITY_PARAMS_LAYOUT` (group
  names, order, size, the binding slot, and that every group is read by a formula) plus
  "the shader hard-codes no galaxy numbers". The formulas themselves are unchanged, so the
  pre-existing parity checks keep passing; executing them is `wgsl-exec-check.js`'s job.
- `sampling-test.js` / `density-distribution-test.js` / `star-types-evolution-test.js` /
  `nebula-placement-test.js`: take the model as the first argument and run on the Milky Way
  preset with unchanged expectations — the numbers they assert are the acceptance criterion.
- `renderer-test.js`: the mode rule — hybrid boots with landmarks and a catalog, `regenerate`
  to a table type empties both blocks and leaves the draw path untouched, `regenerate` back
  re-attaches the same manifest.
- `m1-smoke-test.js` / `export-parity-test.js`: `src/math/galaxy.js` wired like any new module:
  `index.html` (in dependency order), `scripts/run.py` `EXPERIMENTS` **and** `TYPES['test']`,
  the `NAMESPACES`/`BROWSER_GLOBALS` lists and the module table in m1-smoke.

---

## 0.3.1 — Different types & improved star distributions

### The full zoo

`galaxy.js` grows the complete anchor table. Type codes: `E0…E7`, `S0`, `Sa…Sd`,
`SB0…SBd`, `Irr` (≈ T = 9–10): 19 entries in total. The menu reads
`GALAXY_TYPES` (the full table); `G` reads a separate shortlist of E4, S0, SBb, Sc,
Irr. Land the 18 regular types first using the existing spheroid profiles; add Irr
to both lists only when its density and sampler exist. Until then G cycles four.
The regular barred types use the boxy/peanut bar profile below.

The regular table extends the existing E4/S0/SBb/Sc anchors rather than changing the
Milky Way preset. Ellipticals interpolate the model stage through E0 = -5, E4 = -2, E7 = -1
(staying wholly on the B/T = 1, gas = 0 anchors), with `b/a = 1 - 0.1E` and
`c/a = min(b/a, 1 - 0.1225E)` (ordered axes, preserving E4's 0.60/0.51).
Their Sérsic index is 4 through E4 and decreases linearly to 2 at E7. Sa/Sb/Sc/Sd
use T = 1/3/5/6; barred counterparts share each stage's mass, size and population
parameters, and stretch that stage's spheroid into the bar profile (`axes` × 1.8 in the
disc plane, × 0.7 in width, with the boxiness, peanut and end cap from the bar anchors).

### Model parity prerequisites

Every component uses coordinates relative to all three centre coordinates. Empty
model batches contribute no stars to box-restricted sampling. Disc
radii invert `u * F(Rmax)`, not the untruncated CDF at `u`; the vertical inverter
already uses its truncated CDF. Remove the artificial R < 0.01 density plateau,
which has no counterpart in the exponential sampler. Evaluate the thin-disc
`sech²(z/(2H))` as `4e/(1+e)²`, `e = exp(-abs(z)/H)`, avoiding cosh overflow. Spheroid samples multiply
all axes by `r0`. The halo includes its flat core and cuts off at `rMax` in both
JS and WGSL; with `q = 3 - power`, its dimensionless mass is
`1/3 + ((rMax/a_h)^q - 1)/q` (logarithmic tail at q = 0). Invert the core and tail
separately, retaining the same integral for component weights.

Arm density, ridge distances and sampling all read `arms.minRadius` and
`phase0`; the ridge solves `m*phi - K*ln(R/Rs) + phase0 = 2*pi*n` with
`K = m/tan(pitch)` (`density.armWavenumber`, which the sampler and the WGSL mirror
share) and `distanceToNearestArm` returns the perpendicular distance to it.
After overrides, recompute `R0`, `populations.gasRich` and the default home; explicit
home overrides still win. Shape overrides remain literal post-solve edits, not an
implicit mass renormalisation. Test compact discs, halo-only models, non-unit r0,
translated centres and phase-shifted arms, in addition to the preset. Execute the
actual density WGSL against JS; shader text/struct checks alone cannot prove parity.

The radial disc inverter uses the same marginal as the field, including flaring and
central cores. For a disc group `g`, truncation height `Z`, and `H(R) = H0(1 + flare·R/L)`,
the tabulated CDF integrates `R·exp(-R/L)·core(R)·V(H(R), Z)`, where `core(R) =
R/sqrt(R² + coreRadius²)` when enabled and `V` is the exact truncated vertical integral
(`4H·tanh(Z/(2H))` for sech², `2H·(1-exp(-Z/H))` for Laplace). The same quadrature
contract supplies `massIntegrals` and `truncationFractions`; inversion is a fixed-size
monotone table lookup with local interpolation, not a rejection approximation. This
keeps a box-restricted sample conditional on the same field as the full sample.

New shape machinery, one sampler per family (all rejection-free, all deterministic):

1. **Sérsic spheroids** (already in 0.3.0) — E0–E7 vary axis ratio and `n` (4 through E4, down to 2 at E7).
   Use the ordered axis-ratio rule above; no nominal minor axis exceeds the middle axis.
2. **Barred spheroids** — `SB*`: the spheroid grows a bar, the boxy/peanut profile of
   the Milky Way bar (Portail 2016, already cited in plan.md §4). The bar's geometry is
   the existing spheroid group — `a, b, c` are its semimajor, semiminor and vertical
   axes, `n` is the boxiness of its cross-section, `tiltDeg` its position angle — plus a
   `bar: { peanut, endCap, plateau }` group for what only this profile has. In the bar
   frame, in units of the r0-scaled axes:

   ```
   xi = x_b/(a·r0),  eta = y_b/(b·r0),  zeta = z_b/(c·r0)
   P(xi)   = 1 + peanut·xi²                    the peanut: thicker toward the ends
   tau(xi) = (1 - |xi|^n)^(1/n)                the boxy cross-section, pointed at |xi| = 1
   s       = (|xi|^n + |eta|^n + (|zeta|/P)^n)^(1/n)     the level radius, as for
                                                         the other spheroid profiles
   rho     = amp·f(xi)   for  s <= min(1, truncation.spheroidRadius),  else 0
   f(xi)   = 1                                 for  |xi| <= plateau
           = exp(-(|xi| - plateau)/endCap)     beyond
   ```

   The body is *uniform* inside that boxy/peanut cross-section (the "boxy volume") and
   bounded at `|xi| = 1`, so the profile has no tail the ordinary truncation has to
   remove; `plateau` plus the exponential cap is the plan's "boxy/peanut inner part and
   exponential end caps". The sampler is exactly inverted, with no rejection: `|xi|`
   from the CDF of the field's own longitudinal marginal
   (`f(xi)·P(xi)·tau(xi)²`, 1024-bin Simpson, the same quadrature contract the discs
   use), a fair sign, then a uniform point in the cross-section at that `xi` (the
   slice marginal `(1-|y|^n)^(1/n)` plus a uniform coordinate, both tables built from
   the model's boxiness). `massIntegrals`, `truncationFractions` and that CDF read the
   same weight function, so the stars and the field cannot drift apart. Arm `phase0`
   snaps to the bar ends (arms start where the bar ends) — that is what makes SBb read
   as *barred* rather than "Sb with a stick". The Milky Way preset keeps its authored
   Plummer-like bulge (plan.md §4); the boxy/peanut profile is what the table's `SB*`
   rows use.
3. **Flocculent arms** — `arms.flocculence ∈ [0, 1]` blends the smooth `cos(m·φ − …)`
   modulation (grand design, today) with a log-spiral FBM noise field (three octaves,
   hash-based value noise in (ln R, φ) space). Sc/Sd default flocculent ≈ 0.5, Sa/Sb
   grand design ≈ 0.1. `armFactor` becomes a two-term product; `distanceToNearestArm`
   keeps the grand-design ridge line and is used only for young-pop placement (the
   noise term biases, it does not move, the ridge).
4. **Irregulars** — no arms, no symmetry: `rho = rho_base · exp(k_fbm · FBM(x, y, z))`
   with 10–20 hash-placed hotspots (star-forming clumps, radius ~ 0.3 kpc, density
   boost ×3) and a mild global rotation (added in 0.4 via `dynamics.vFlat ≈ 50 km/s`).
   The sampler becomes two-stage: draw from the smooth base (disc-like, large H/L),
   then apply clump assignment by hash (a star near a hotspot is pulled toward it by a
   fixed factor) — cheap, stable, and the density test measures the resulting field
   against the same rule.

### Improved star distributions

What "improved" means concretely, beyond the type shapes above:

- **Population gradients.** `star-types.js` already ages stars by component and arm
  distance. Add radial metallicity, as a *colour* modifier only (no new physics):
  thin-disc stars dim/redden toward the edge (`Δ colourIndex` up to +1 step at
  `2·L`), spheroid stars metal-poor from birth (bluer turnoff + redder giants —
  approximated by shifting 20% of spheroid giants one LUT step redder). Gradients are
  per-type: flat in E, steep in Sc.
- **Arm-hugging young population.** O/B placement tightens from "near an arm" to a
  proper ridge distribution: the thin-disc age draw becomes the gate `exp(-z²/2)` with
  `z = distToArm / armRidgeWidth(model, R)`, so how likely a star is to have been born
  young falls off as a half-normal from the ridge line, and the young population is a
  half-normal in z for every type (measured per star: median 0.65, 70 % inside 1 σ, 96 %
  inside 2 σ). The width is the pattern's own, in `density.js`:

  ```
  λ(R) = 2·π·R·sin(pitch) / m = 2·π·R / hypot(m, K)   the ridge spacing, perpendicular
  armRidgeWidth(model, R) = 0.12 · λ(R) / (1 + amp)   K = m/tan(pitch)
  ```

  `ARM_RIDGE_FRAC = 0.12` is the newborn lane's fraction of the arm spacing and `amp` is
  the arm contrast (a stronger arm compresses its gas harder). The width therefore scales
  with radius — arms widen outward — and with the galaxy's size, and the Milky Way's own
  numbers land on ~0.3 kpc in the inner disc, the scale the branch was tuned to. The gas
  layer reads the same width (`nebula.js`: HII within one ridge width, reflection at 2.4,
  dark clouds at 4.6, and the arm probability boost is the same Gaussian), so the young
  stars and the gas clouds trace one lane instead of two different ones. OB *clustering*
  lands in 0.3.2 as objects; this item is the smooth version.

  Two tests pin the geometry the lane rides on, on different measurements: the crest's
  *winding* (its azimuth at two neighbouring radii, which recovers the anchor pitch to
  0.05°) and the crest *spacing* along a ray (consecutive crest radii come out
  `e^(2π/K)` apart, recovering `λ` to 0.03 %). Both matter because the lane's width,
  `λ·0.12/(1+amp)`, is only meaningful if `λ` is the spacing the field has, and because
  `K` had been written `tan(pitch)` — a fan of radial spokes, where the "pitch angle"
  is not the angle of anything. `distanceToNearestArm` returns the perpendicular distance
  for the same reason: an azimuthal arc is `1/sin(pitch)` times the distance the gate's
  σ is written in.
- **Vertical structure.** Disc flaring: scale height grows with R (`H(R) = H0·(1 + flare
  · R/L)`), thin and thick separately. E/S0 spheroids keep the anisotropic flattening
  from the type table.
- **Radial profile edge cases.** Late types get a central hole or flat core option
  (`disc.coreRadius`, used by Sd/Irr — real Sd galaxies show it); early types keep the
  cusp. The sampler's radial CDF includes the soft-core factor and flaring rather than
  replacing either with a cutoff or a flat-height exponential.
- **Density parity everywhere.** Every new profile gets the sampler ↔ density parity
  treatment in `density-distribution-test.js` (run per type). The truncation rule is
  unchanged and applies per component: nothing is sampled where the field reads zero,
  and every derived placement (nebulae, objects, gap-fill) reads the same field.

### Tests (0.3.1)

- `galaxy-types-test.js` extends to the full table: monotone trends across T for B/T,
  pitch, gasFraction, H/L; bar geometry sane (length < disc radius, arms attach at the
  ends within tolerance); flocculence monotone in T for the Sc–Sd sequence.
- `density-distribution-test.js` runs the parity suite for {E4, S0, SBb, Sc, Irr} plus a
  table bar (SBa), so every new profile — including the boxy/peanut bar, which the SBb
  preset does not exercise — is measured against the field (component shares within 2%
  of the analytic masses; radial and vertical histograms within tolerance).
- `model-parity-test.js` covers the bar's own shape: the major-axis marginal, the
  vertical marginal (peanut included), the exponential end cap and the pointed tip.
- `star-types-evolution-test.js`: metallicity-gradient checks (mean colour index rises
  with R in discs, flat in E), young fraction tracks `gasFraction` and arm proximity, and
  the arm-young population is a half-normal in `distToArm / armRidgeWidth(model, R)`
  (Sa and Sc) with the ridge itself the pattern's — tighter in Sa than in Sd at one
  radius, widening in proportion to R. `nebula-placement-test.js` pins the gas side: the
  arm probability falls by `e^-1/2` exactly one ridge width off the crest.

---

## 0.3.2 — Objects like nebulae, with proper star distributions

`nebula.js` places loose nebulae from `pGas`/`pStellar` today. Real nebulae are not
loose: an HII region *is* its OB association, a reflection nebula sits on its B stars,
a globular cluster is a stellar system that happens to be spectacular. 0.3.2 turns
placement into **composite objects**, each with an internal star distribution.

### Object catalogue

| Object | Gas | Members | Internal distribution | Population |
|---|---|---|---|---|
| HII region / OB association | HII or reflection nebula billboard | 20–200, uniform | Fractal: uniform candidates in the 2.5·R_e sphere, kept where 3-octave value noise > 0 (≤ 10 deterministic retries), R_e 50–150 pc | coeval, age 1–20 Myr uniform: O/B + A/F |
| Open cluster | none (or residual reflection) | 30–300, uniform | Plummer `r = a/√(u^−2/3 − 1)`, a = 1 pc, tidally cut at 10–30 pc | coeval, age 10 Myr–1 Gyr log-uniform: thin-disc mix |
| Globular cluster | none | 200–2000, uniform | King W0 ≈ 6 as `ρ ∝ ((1+x²)^−1/2 − (1+12²)^−1/2)²`, x = r/r_c, r_c 1–3 pc | coeval, age 10–13 Gyr uniform: RG + WD + low-mass, no O/B |
| Planetary nebula | planetary (green [OIII]) billboard | 1 | point (the central star) | post-AGB: T_eff 30–100 kK, L 10²–10⁴ L☉ |
| SNR | shell billboard | 0 | — | — |

### Placement

`placeObjects(model, seed, N, box)` in `src/math/objects.js` (new) generalises
`placeNebulae` — same candidate scheme (sample from the field in 4096-star batches,
accept by probability; `box` null means the whole galaxy, which is what the renderer's
objects block uses). Probabilities extend the existing two terms; `gasWeight` is
`gasFraction / 0.15` gated by `gasRich`, `inDisc = exp(−|z| / thin.H·youngScaleHeight)`,
`armBoost` the nebula lane's Gaussian (0 past 2.4 ridge widths), and the bulge/halo
suppressions are the nebula layer's:

```
pHII       = min(1, 0.05 ·gasWeight·inDisc·armBoost·bulgeSuppress·haloSuppress)  // == pGas; ageGate is 1 until 0.3.3
pOpen      = min(1, 0.020·gasWeight·inDisc·bulgeSuppress·haloSuppress)           // disc-wide, no arm requirement
pGlobular  = min(1, 0.010·(0.2 + 2·spheroidFrac)·(bulge? 3 : halo? 1 : 0.15))
pPlanetary = pStellar = 0.005·(bulgeDensity > 0.05? 4 : 1)·(halo? 0.3 : 1)       // unchanged
pSNR       = pStellar·thinFrac·(0.5 + armBoost)                                 // thinFrac = thin/(thin+thick+bulge+halo)
```

`spheroidFrac` is the delivered spheroid share (`deliveredMasses`), computed once in
`objectContext(model)` — the per-candidate path must not run mass quadrature. The
candidate is accepted with p = min(1, Σ) and its type is rolled proportionally, so a
quenched type (gasWeight 0) yields globulars, planetaries and SNR only, and a gas-rich
disc yields mostly HII regions and open clusters. An empty model (no delivered mass)
yields no objects rather than looping on empty batches.

Each accepted object hashes `(seed, index)` → type roll, size, richness, age, internal
seeds. Everything stays a pure function of the hash — the AGENTS.md stability rule
applies to members: member *i* of object *j* is `(seed, j, i)` through the same
IMF pipeline as field stars (`star-types.js` with the object's age override: HII/open
members are thin-disc stars coeval at the object age, globular members are halo stars
coeval at the object age, the planetary central star is a dedicated post-AGB
derivation). Positions are centre + profile offset; R and arm distance are recomputed
at the member's own position so the metallicity gradient reads correctly.

### Proper member distributions

Members are generated by inverting the object profile — no unbounded rejection:

- Plummer: `M(<r) = r³/(r²+a²)^3/2`, so `r = a/√(u^−2/3 − 1)` closed form, uniform
  direction, tidally truncated by scaling `u` into `M(<r_t)`.
- King W0 ≈ 6 (concentration c ≈ 1.1, `x_t = 12`): `ρ(x) ∝ ((1+x²)^−1/2 −
  (1+x_t²)^−1/2)²` with `x = r/r_c`, numeric CDF by 1024-bin Simpson and table
  inversion — the same quadrature contract the discs and the bar use — uniform
  direction.
- Fractal OB: uniform candidate in the `2.5·R_e` sphere, kept where the 3-octave
  value noise (`density.fbm3D` at the object's seed) is above 0, with up to 10
  deterministic retries and the last candidate as fallback so the count is exact.
  The retries are a fixed hash sequence, so member *i* is still a pure function of
  `(seed, j, i)`.

Mass segregation is deferred: biasing masses low → large radii needs a rank coupling
across an object's members, which the per-member `(seed, j, i)` purity does not have.
It reads as "real cluster" at close range and stays optional polish for later.

### Rendering and budget

- Members are ordinary `StarPacked` records in a new fixed **objects block** of the
  star buffer: `[global][landmarks][objects][local gap-fill][catalog]`. The objects
  block sits with the fixed-on-regenerate blocks rather than between the two
  per-rebuild ones, so regenerate uploads one contiguous fixed range and each rebuild
  uploads one contiguous dynamic range — the draw is one contiguous range either way.
  Written on regenerate, not per rebuild. Budget: `OBJECT_MEMBERS_DEFAULT = 20000`
  (320 KB) — same order as the gap-fill block; drawn always (they are point stars;
  unfilled capacity is zero records, culled by `MASK_VISIBLE`). The renderer places
  `OBJECT_COUNT_DEFAULT = 400` objects whole-galaxy; richness is a weight, and the
  20k slots are apportioned over the placed objects (cumulative rounding, every
  non-empty object keeps at least one member), so every object is represented, a
  globular renders ~10× the members of an association, and the budget is exact for
  every type.
- Nebula gas renders as billboards (plan.md §10 tier 1) in their own additive pass
  after the sprites: camera-facing quads, size from `size`, colour from
  `NEBULA_COLORS`, soft radial alpha (a tiny fragment falloff, no texture files).
  Cull by screen-size (skip below ~4 px) and distance (beyond 5 kpc a nebula is a
  smear — drop it). Shell/volumetric tiers stay in plan.md §10 / Milestone 6; 0.3.2
  ships billboards only, which is what "objects like nebula" needs.
- The 2D label layer can name the top objects (HII region, cluster) — optional.

The sun-relative Hybrid mode gets object placement too (the real Orion nebula is a
landmark; procedural HII regions around it are allowed — the thin disc there is the
same field).

### Tests (0.3.2)

- `experiments/object-placement-test.js` (new): object density follows `pHII`/etc.
  (measured by R, |z|, arm distance buckets — the `nebula-placement-test` harness);
  member histograms match Plummer/King within tolerance and the fractal keeps its
  threshold (≥ 99% of members where the noise is above 0) inside `2.5·R_e`; member
  stability (same seed ⇒ byte-identical members); type shares vs `gasFraction` (E:
  zero HII/open; Sc: HII+open the majority); `OBJECT_COUNT_DEFAULT` whole-galaxy
  objects fill `OBJECT_MEMBERS_DEFAULT` for every type in `GALAXY_TYPES`.
- `nebula-placement-test.js` keeps passing unchanged for the loose-probability layer.
- `renderer-test.js`: objects block layout + instance counts (0.3.2a); the billboard
  pass, drawn and culled below the size floor, lands with 0.3.2b.
- Budgets asserted: `sum(members) ≤ OBJECT_MEMBERS_DEFAULT` always.

---

## 0.3.3 — Galaxy age

The final parameter: `populations.age` in Gyr (0.2 – 13.5, menu slider + `?age=`).
Age does not animate the galaxy (0.4 moves stars; nothing *evolves* them over scrub
time in 0.3) — age is the **generation parameter**: at what cosmic epoch this galaxy
is frozen.

`AGE_REF = 13.5 Gyr` is three things at once, deliberately: the slider's ceiling
(nothing in the engine can be older than the universe), the epoch the type table
describes (every anchor in `galaxy.js` is an observed present-day value), and the
default age. A model built without an `age` is therefore the galaxy 0.3.0–0.3.2
pinned, which is what makes "the default sky does not move" a testable claim rather
than a hope.

### Star-formation history per type

One SFH per type, in formation time `t_f` counted from the galaxy's own first light:

```
SFR(t) ∝ t · exp(−t/τ)     on  0 ≤ t ≤ span,    span = min(age, quenchTime)
τ (Gyr):   E 0.4 (monolithic burst then dead) · S0 1.0 · Sa 3 · Sb 5 · Sc 8 · Sd 12 · Irr 15 (≈ constant)
quenching: quenchTime = 1.5·τ for the stages with T ≤ 0 (the E sequence and S0); 0 = never for the rest
```

`τ` is an anchor table over the de Vaucouleurs stage (`ANCHORS.TAU_SFH`) like every
other population parameter, so the preset's 5.0 is the Sb stage value and E7/Irr
clamp at the table's ends. A star's age is `age − t_f`, and everything `evolveStar`
already does with an age — IMF mass → main-sequence lifetime → O/B gone after
40 Myr–0.4 Gyr, giants, remnants — now runs on the galaxy's clock.

**Components are formation windows, not age priors.** `sampleLocalAge` today draws a
lognormal per component (halo 12 Gyr, bulge 10, thick 8, thin 5, arms 0–0.3). Those
are the *Milky Way's* ages at one epoch: a 1 Gyr galaxy cannot honour a 12 Gyr halo,
and a 13.5 Gyr irregular cannot honour a 5 Gyr disc. 0.3.3 replaces them with a
window on `t_f`, as a fraction of `span`, so the assembly order survives at any age
and every component is young when the galaxy is:

```
FORMATION_WINDOW (fraction of span):   halo [0, 0.25] · bulge [0, 0.45] · thick [0.10, 0.65] · thin [0.25, 1.00]
```

The window is a CDF interval, precomputed at build into `populations.formQ`
(`[lo, hi]` per component, in the order of `COMPONENT_*`):

```
q   = mix(lo, hi, u1)
t_f = τ · F⁻¹(q · F(span/τ)),     F(x) = 1 − e^−x (1 + x)      (galaxy.sfhCumulative)
```

One function `F` for the whole release: the windows, the gas law below and the
shader's bisection all read it, and the CPU's inverse table is *built from* it, so
there is no second copy of the distribution to drift.

The windows are calibrated on the preset, not guessed: at `AGE_REF` they reproduce
the mean component ages of the priors they replace — thin 4.62 (was 4.58), thick
8.32 (8.36), bulge 9.91 (10.10), halo 11.40 (11.99, on a 23-star sample) — and the
field's mean luminosity to 0.7% (111.0 against 111.8), which is what keeps the
exposure defaults meaningful at the default age.

The arm-young branch stays exactly as 0.3.1 built it: a half-normal gate on
`distToArm` in units of `density.armRidgeWidth`, `u1³ · 0.3 Gyr` inside the
star-forming annulus, available only when `gasRich`. It is the O/B source and its
shape is pinned by `star-types-evolution-test.js`; the only change is that its
ceiling is clamped to the galaxy's age (`min(age, u1³ · 0.3)`), so a 0.2 Gyr galaxy
contains no 0.3 Gyr stars. No gas or SFR factor is applied to the gate: with the
reference-epoch gas law below, every reachable age has at least the reference gas,
so a continuous boost is capped at 1 for every type that has an arm branch at all —
it could only re-tune the Sa, which is a type-table question (0.3.1), not an age one.

**`F⁻¹` is a table on the CPU and a bisection on the GPU.** The truncated delayed
exponential has no elementary inverse (it is a Lambert W), so the two sides solve it
differently, on purpose. Measured: a 1025-entry per-model inverse table, built once
per regenerate in 3.8 ms, answers a draw with two array reads — 2.8 ms per 300k
stars, rms error 6.3e-4 Gyr against a 60-step reference solve. The same draw by
14-step bisection on the analytic `F` costs 91 ms per 300k stars, half the derive
pass, on the age slider's hot path. So `star-types.js` indexes a table (cached on
`(tauSfh, sfhSpan)`, rebuilt only when the clock changes) while `procedural-gen.wgsl`
bisects `F` in `SFH_BISECT_STEPS = 14` — a shader cannot carry a per-model table, and
the compute path generates a cell at a time where 14 exp calls are free.
`wgsl-exec-check.js` consequently splits its `sampleLocalAge` probes: the closed-form
arm branch keeps its 2e-3 relative check, the SFH branch is compared on a 0.05 Gyr
absolute floor (the table's 6e-4 rms plus an f32 bisection's slack in `F`'s flat
tail; nothing downstream reads age at finer resolution — states flip on
main-sequence lifetimes).

### Gas depletion

`populations.gasFraction` keeps the meaning the anchor table has always had: the
observed present-day fraction at `AGE_REF` (Roberts & Haynes 1994). What the model's
age changes is how much of it is left.

The draft's `gasFraction · exp(−age/2τ)` cannot be used as written, and the reason is
worth keeping: read as a *primordial* fraction it needs values above 1 to reach the
observed present-day numbers of the early types (an S0 at τ = 1 would have to start
at 17), and read as a *present-day* fraction it makes a quenched galaxy gas-richer
the younger it is, which is backwards — quenching means the gas is already gone.
Instead the gas is what the SFH has not yet turned into stars, which needs no new
constant, is bounded by construction and reads the same `F` the ages do:

```
M*(t)  = F(min(t, quenchTime)/τ) / F(min(AGE_REF, quenchTime)/τ)      fraction of the galaxy's stars already formed
gasNow = gasFraction + (1 − gasFraction) · (1 − M*(age))
```

`gasNow ≤ 1` and `≥ gasFraction` by construction, `gasNow = gasFraction` exactly at
`AGE_REF` (so every number 0.3.0–0.3.2 pinned survives), monotone falling in age,
and flat after quenching — an S0 keeps its 0.02 residual instead of inventing gas,
while an E4 mid-burst at 0.5 Gyr has 0.196 of its mass still cold. Measured:

| type | τ | 0.5 Gyr | 2 | 8 | 13.5 |
|---|---|---|---|---|---|
| E4 | 0.4 | 0.196 | 0.000 | 0.000 | 0.000 |
| S0 | 1.0 | 0.800 | 0.020 | 0.020 | 0.020 |
| Sa | 3.0 | 0.988 | 0.859 | 0.270 | 0.080 |
| SBb | 5.0 | 0.995 | 0.930 | 0.463 | 0.150 |
| Sc | 8.0 | 0.997 | 0.963 | 0.632 | 0.300 |
| Irr | 15.0 | 0.999 | 0.982 | 0.779 | 0.500 |

`gasNow` is what `gasFraction` fed: `gasRich = gasNow ≥ GAS_RICH_MIN`, and
`gasWeight = gasNow / GAS_NORMAL` in `nebula.js` and `objects.js`. `GAS_NORMAL` moves
to `galaxy.js` beside `GAS_RICH_MIN` (both are model-level gas normalisations, and
the population layer must not have to load the nebula layer to read one); `nebula.js`
and `objects.js` read it from there. Composite-object ages are clamped to the
galaxy's (`ageGyr = min(ageGyr, age)`): a 1 Gyr galaxy has no 13 Gyr globular.

Measured object mixes, 400 whole-galaxy objects (HII / open / globular / planetary /
SNR — the total is the budget, so age moves the *mix*):

| | 0.5 Gyr | 2 Gyr | 8 Gyr | 13.5 Gyr |
|---|---|---|---|---|
| SBb | 107/151/36/73/33 | 105/152/37/73/33 | 75/118/53/105/49 | 41/39/80/172/68 |
| Sc | 135/167/11/56/31 | 132/167/9/58/34 | 118/143/9/85/45 | 88/114/22/110/66 |
| E4 | 0/5/345/50/0 | 0/0/349/51/0 | 0/0/349/51/0 | 0/0/349/51/0 |

### Visual consequences (what the slider must visibly do)

Measured on 120k preset stars: thin-disc O/B %, thin-disc giants %, thin-disc stars
younger than 0.3 Gyr %, and the field's mean luminosity in L☉.

| age | O/B | giants | young | mean L | reads as |
|---|---|---|---|---|---|
| 0.5 Gyr | 0.83 | 0.09 | 93 | 9.9 | blue-white disc, gas everywhere, no giants yet |
| 2 Gyr | 0.53 | 0.37 | 41 | 28.8 | |
| 8 Gyr | 0.23 | 1.18 | 22 | 78.9 | |
| 13.5 Gyr | 0.20 | 1.75 | 20 | 111.0 | today's sky: O/B only on the freshest arms, RGB sparkle |
| E4 at 0.5 Gyr | 0.55 (spheroid) | 0.29 | — | | the blue E the draft promised |
| E4 at 13.5 Gyr | 0 | 3.45 (spheroid) | 0 | | red spheroid, dead: every star 12.9–13.5 Gyr old |

**White dwarfs do not accumulate with age in this model, and the draft's row saying
they do was wrong.** `evolveStar` makes a remnant of a star past its main-sequence
lifetime only above 8 M☉, whose lifetime is at most 0.24 Gyr, so the WD fraction is
0.15% at 0.5 Gyr and 0.18% at 13.5: it rises and saturates below 1 Gyr. The honest
channel — intermediate-mass stars dying to WDs after a giant phase that lasts ~10% of
their MS life — would move about 90% of the field's light, because today *every* dead
low-mass star is a giant forever and those giants carry 99% of the flux (110 of the
111 L☉ above). That is an exposure re-tune which needs a browser to judge, so it is
0.5 material; the test pins the saturating behaviour instead of the fiction. The
monotone age tracer is the giant fraction (0.09 → 1.75%, a factor 19).

### Exposure renormalisation

Age changes the mix, and the mix is what the exposure defaults were tuned on: the
field's mean luminosity falls 11× (2.62 mag) from 13.5 Gyr to 0.5 Gyr. The renderer
measures its own field and folds the difference into the packed magnitudes, so the
sliders stay where the user left them:

```
magOffset = 2.5 · log10( meanL(model) / meanL(same model at AGE_REF) )      [mag, added to absMag]
```

Both means come from the same first 16384 sampled positions
(`starTypes.meanFieldLuminosity`), so the reference half is a property of the galaxy's
geometry and is computed only when the field is re-sampled, not on every slider step.
At `AGE_REF` the two halves are the same model, so `magOffset` is exactly 0 for every
type: the exposure the user tuned at the default age is untouched, and the default
field keeps 0.3.2's brightness (its mean luminosity reproduces the old priors' to
0.7%, by calibration of the windows). Individual stars are not byte-identical to
0.3.2 — their ages now come from the SFH rather than from per-component lognormals,
which is the change the release is for. The offset applies to the procedural blocks
(global field, object members, local gap-fill); landmarks and catalog stars are real
data and keep their real magnitudes — sliding the age on the preset is a what-if over
the procedural field, not a claim about Gaia.

### UI

```
age         [ ⟨————●————⟩  13.5 Gyr ]     — slider 0.2–13.5, step 0.1, throttled live regenerate
```

Live, but throttled: the readout follows the drag, the field rebuilds at most every
`AGE_REGEN_INTERVAL = 0.35 s`, and the pending value is applied from the frame loop so
the last position of the drag always lands (no trailing-edge timer, no queued
rebuilds). An age change does **not** move the camera, drop the selection or touch the
label layer — it is the same galaxy at another epoch, and teleporting the viewer home
on every slider step would make the control unusable. Overlay:
`galaxy SBb #42   age 13.5 Gyr   hybrid mode`. URL: `?age=2`.

### Property-only regenerate

Changing age re-rolls star *properties*; positions are age-independent in 0.3. So
`renderer.regenerate(model)` keeps the sampled field when the geometry is unchanged and
rewrites only the records:

- `galaxy.sameGeometry(a, b)` compares the packed uniform's **geometry groups** —
  everything except `populations`, `clock`, `formLo`, `formHi`. That set is exactly
  the numbers `sampleGalaxyStars` reads (masses, truncation, arms, centre, clumps), so
  the test is the sampler's own input list rather than a proxy like `type === type`
  that an override would silently defeat.
- On a match: recalibrate the offset, re-derive the records, re-place the objects
  (their rates read `gasNow`), rewrite the fixed block, re-upload it. The landmarks
  keep their bytes in staging, the catalog and its residency set are untouched.
- On a mismatch (type, seed, override): the full `prepare` path, as before.

Measured at 300k stars: the full path is 0.58 s (sample 0.32 s + derive 0.195 s +
objects 0.064 s), the property path 0.27 s (derive 0.195 s + objects 0.064 s +
calibration 0.006 s + the SFH table 0.004 s). `state.fieldSamples` counts full
position draws so the reuse is observable rather than implied.

### Tests (0.3.3)

- `galaxy-age-test.js` (new):
  - the SFH: `t_f` over the whole span matches the truncated delayed-exponential CDF
    within 0.02 (Kolmogorov–Smirnov on 20k draws) for every type at three ages; its
    mean matches the analytic truncated mean `τ·[2 − e^−X(X²+2X+2)]/F(X)`; `t_f` never
    exceeds `span`; quenched types never draw past `1.5·τ`; a wider `span` never makes
    a component younger than its window allows.
  - the windows: each component's `t_f` stays inside its own `[lo, hi]`; mean star age
    orders halo > bulge > thick > thin at `AGE_REF` and every component is `≤ age` at
    `age = 1`; the preset's default-age means reproduce the priors they replace.
  - the population: the thin-disc O/B fraction falls with age (≥ 0.5% at 1 Gyr,
    ≤ 0.25% at 13.5); the giant fraction rises monotonically (≥ 5× from 0.5 to
    13.5 Gyr); the mean colour index rises with age; the remnant fraction is
    non-decreasing and saturates below 1 Gyr (the honest WD statement, above).
  - the gas law: `gasNow = gasFraction` at `AGE_REF` for every type; monotone falling;
    `≤ 1`; flat past quenching; E4/S0 below `GAS_RICH_MIN` at `AGE_REF` and above it
    at 0.5 Gyr; `gasRich` follows `gasNow` and not `gasFraction`.
  - the objects: HII + open counts fall with age at fixed type; no object is older
    than its galaxy; globular ages clamp to `age`.
  - the renormalisation: after `magOffset`, the calibrated mean luminosity is within
    1% of the reference at every age for every type.
- `star-types-evolution-test.js` gains the age-distribution mode (component windows ×
  the galaxy clock) and keeps its current checks on the default model.
- `galaxy-types-test.js`: the clock groups in the uniform, `sameGeometry`'s verdicts
  (an age change matches, an override does not), and the `sampleLocalAge` probes
  re-read against the SFH.
- `renderer-test.js`: an age-only regenerate keeps the sampled field (`fieldSamples`
  unchanged), rewrites the fixed block with identical positions and different colour
  bytes, reports `magOffset`, and leaves the catalog and landmarks alone.
- `wgsl-validate.js`: the three new groups exist and are read, `sfhCumulative` is
  mirrored from `galaxy.js`, `YOUNG_ARM_MAX_GYR` matches the WGSL const.
- `wgsl-exec-check.js`: the executed `sampleLocalAge` matches JS — the arm branch to
  2e-3 relative, the SFH branch to 0.05 Gyr.

---

## Implementation order

1. **0.3.0a** `src/math/galaxy.js`: descriptor + anchor table + Milky Way preset that
   reproduces today's constants bit-for-bit; `density.js` takes the model;
   `DENSITY_PARAMS` uniform + `wgsl-validate` struct parity.
2. **0.3.0b** `E4`/`S0`/`Sc` shapes (sersic inverter, arm pitch/amp through the table,
   gas gating in star-types/nebula), mode rule, home positions, UI section, `G` key.
3. **0.3.1a** regular type table + model parity prerequisites; then the boxy/peanut bar
   + arm phase0 coupling, flocculent arms, irregular FBM + hotspots.
4. **0.3.1b** population gradients, arm-ridge young placement, flaring, disc cores;
   per-type parity suite.
5. **0.3.2a** `placeObjects` + internal profile inverters + objects block in the star
   buffer (no gas rendering yet — objects as star concentrations already look right).
6. **0.3.2b** nebula billboard pass + size cull + budgets.
7. **0.3.3** SFH sampling (delayed exponential + per-component formation windows),
   mass-conserving gas depletion, age slider with a property-only regenerate, exposure
   renormalisation.

Steps 1–2 ship 0.3.0; 3–4 ship 0.3.1; 5–6 ship 0.3.2; 7 ships 0.3.3. Tests land with
each step, per the AGENTS.md workflow.

## Out of scope

- Isochrone-level stellar physics (real SPS tracks, dust radiative transfer). The
  population model stays "IMF + lifetime + LUT", just with honest age distributions.
- The intermediate-mass white-dwarf channel: a giant phase lasting ~10% of the
  main-sequence lifetime and then a WD, so white dwarfs accumulate with age. Today
  every dead low-mass star is a giant forever and those giants carry 99% of the
  field's light, so the honest split is an exposure re-tune that needs a browser to
  judge (§0.3.3 measures the 11× swing it would have to absorb).
- Supernova-remnant rates from the SFH's instantaneous value (`pSNR` is stellar-mass
  driven, so it does not fall when star formation does). Same reason: it moves the
  default-age object mix.
- Evolution *over* scrub time: moving the age slider is a regenerate, not a
  time-lapse of stellar evolution. Combining age with 0.4's star time (stars ageing as
  T advances, births along arms) is a possible 0.5, deliberately not here.
- Mergers, tidal tails, satellite galaxies, cD envelopes.
- Textured / volumetric nebulae (plan.md §10 tiers 2–3 remain the target for hero
  objects).
- Per-type music or marketing names for the galaxies ("Andromeda-ish" etc.). Type
  codes only.
